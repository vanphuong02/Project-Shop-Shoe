import { LimitToPipePipe } from './limit-to.pipe.pipe';

describe('LimitToPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LimitToPipePipe();
    expect(pipe).toBeTruthy();
  });
});
